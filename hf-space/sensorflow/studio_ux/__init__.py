"""Studio-UX support module: dashboard layout persistence + read-only
visualization replays for the WYSIWYG frontend. Never mutates other
subsystems' state; only imports their pure generation/evaluation functions."""
